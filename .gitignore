# macOS
.DS_Store

# Wrangler local state (contains auth tokens, do not commit)
.wrangler/

# Legacy direct-upload artifacts (no longer used — Git-connected Pages deploys from root)
vanguardeer-deploy/
vanguardeer-deploy.zip
vanguardeer-deploy-new.zip

# Private business data
*.xlsx
*.csv

# Accidental nested repo path — never commit
Claude/

# Disposable patch/working scripts
patch_*.py
